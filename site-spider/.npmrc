# The Native SDK CLI is pinned in package.json and may be newer than the
# machine-wide npm release-age policy.
min-release-age-exclude[]=@native-sdk/*
min-release-age-exclude[]=scriptc
min-release-age-exclude[]=@scriptc/*
